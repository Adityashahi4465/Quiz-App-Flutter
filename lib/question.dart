class Question {
  String questionText;
  bool questionAnswer;

  Question(this.questionText, this.questionAnswer);
  // {
  //   questionText = q;
  //   questionAnswer = a;
  // }
}

// Question newQuestion = Question('text', true);
